stage('Build Containers') {
    steps {
        sh 'docker compose -f docker/docker-compose.blue.yml up -d --build'
        sh 'docker compose -f docker/docker-compose.green.yml up -d --build'
    }
}